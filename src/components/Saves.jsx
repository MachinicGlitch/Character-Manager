import React from 'react'
import PropTypes from 'prop-types'

function Saves() {
    return (
        <div>
            <h2>Saves</h2>
        </div>
    )
}

Saves.propTypes = {
}

export default Saves